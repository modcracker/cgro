import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contact Administrative Escrow Agent | CalGro.com Acquisition',
  description: 'Submit an acquisition inquiry through the secure administrative escrow system. Connect with administrative managers of calgro.com.',
  keywords: 'calgro contact, contact escrow agent, domain liaison admin, buy calgro.com',
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
