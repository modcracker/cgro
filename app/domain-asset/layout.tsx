import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'CalGro.com Premium Domain Valuation & Purchase Terms',
  description: 'Inquire about current escrow valuation criteria, GoDaddy Premium Brokerage listings, and transfer agreements for acquiring the brand name calgro.com.',
  keywords: 'calgro.com, buy calgro, calgro domain asset, godaddy premium escrow, domain transfer brokerage, premium domain asset',
};

export default function DomainAssetLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
